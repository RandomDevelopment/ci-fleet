# Replace the mutable example base with a reviewed digest before production use.
FROM node:22-bookworm-slim

WORKDIR /workspace

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

CMD ["npm", "test"]
